# URL of the StockYard backend (see server/). Used for all REST API calls.
VITE_API_URL=http://localhost:4000

# WebSocket URL for real-time updates — usually the same host as above, ws:// instead of http://
VITE_WS_URL=ws://localhost:4000
